const express = require('express');
const router = express.Router();
const Logs = require('../utils/Logs');
const { db, Connect, Stop } = require('../config/database');

// TODO: Admin related functions

module.exports = router;